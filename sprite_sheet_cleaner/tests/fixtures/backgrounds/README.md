# Background-removal fixtures

Fixtures in this directory remain small and repository-safe. Prefer deterministic images generated in tests for algorithm coverage. Add hand-authored PNGs only when they are necessary for a golden-image regression, and never add model weights or generated caches.
